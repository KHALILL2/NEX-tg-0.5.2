# Gate Access Monitoring System — Python Dependencies
# Install:  pip install -r requirements.txt
#
# RPi-only packages (spidev, mfrc522) are listed under [rpi].
# On dev machines, install the base deps only:
#     pip install -r requirements.txt
# On Raspberry Pi:
#     pip install -r requirements.txt -r requirements-rpi.txt

customtkinter>=5.2.0,<6.0
Pillow>=10.0.0,<12.0
requests>=2.31.0,<3.0
urllib3>=2.0.0,<3.0
pyserial>=3.5,<4.0
arabic-reshaper>=3.0.0,<4.0
python-bidi>=0.4.2,<1.0
